{
  "name": "shops",
  "version": "1.0.0",
  "description": "an api for an application that finds shops nearby ",
  "main": "index.js",
  "directories": {
    "lib": "lib"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "concurrently --kill-others \"mongod --dbpath ./data\" \"node index\""
  },
  "author": "Badou",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^4.1.0",
    "mongodb": "^3.1.13"
  }
}
